import Header from './Header.svelte';

export default {
  title: 'components/common/Header',
  component: Header,
};

const Template = () => ({
  Component: Header,
});

export const Default = Template.bind({});