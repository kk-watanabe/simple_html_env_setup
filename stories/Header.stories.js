import Header from './Header.svelte';

export default {
  title: 'common/Header',
  component: Header,
};

const Template = () => ({
  Component: Header,
});

export const Default = Template.bind({});
